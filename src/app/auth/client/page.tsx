import { ClientAuthWrapper } from "../../../entities/user/presentation/client-auth-wrapper";

/**
 * Client authentication page
 * Provides WhatsApp OTP authentication for clients
 */
export default function ClientAuthPage() {
  return <ClientAuthWrapper />;
}
